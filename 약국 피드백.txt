서버는 node
클라이언트는 html로 작업함

네이버 api가 리액트보다 html 환경에 최적화 되어 있어서 다음에 할때에는 리액트로 구성한 뒤에 index 폴더에 들어가서 api 가져오는 식으로 진행해야될 듯

getlocation 같은 지리 정보를 통해서 현재 위치값을 구할 수 있음

위치 정보 표시(getCurrentPosition)는 비동식 함수 호출임

getlocation 함수 실행 시, 비동기 식이기 때문에, 데이터가 먼저 들어옴, 
동기식으로 순서를 지정 시켜주어야함.

new Object의 역할은 말그대로 객체를 생성하는 함수,,, 객체를 생성하면?
프로퍼티를 생성 할 수가 있음

만드는 순서에 대해서,,

1. 먼저 node express를 먼저 생성해서 서버를 구동
- index.js를 통해서 포트 생성 및 app.use를 통해서 (public-html)폴더를 파싱함

2. public-html에 있는 index.html에서 네이버 open api script를 가져옴
- 클라이언트 id란에 복붙,
- 네이버 map함수를 html 양식에 맞게 화면서 송출
- 위치기반 약국 api 연동이기 때문에, 위치 값을 받아오는 함수 사용
- 위치 값을 받아오는 비동기 함수를 promise를 사용하여, 동기함수로 변환
- 이후 똑같이 네이버 지도를 받아오는 함수에 async를 붙여서 위치값 호출

3. 약국 API를 가져오는 과정
! 서버에서 각자 다른 도메인을 가져오기 때문에, cors 에러가 날 수 있음.
해당 과정을 node.js를 통해서 방지 해주어야함.

! api 변수를 선언해서 가져오는 이유는 cors 보안 문제를 해결하기 위해서 그러는거임

? response.setheader는 HTTP 응답 헤더를 설정하는 Node.js 메서드입니다.

? "*"는 가능한 모든 것을 해결해줘 라는 키워드

-node 내 axios를 require로 선언하고, try catch를 통해서 에러처리를 해주어야한다. 

- 해당하는 도메인을 app.get을 통해서 받아주어야함 그것도 비동기함수로 원하는 페이지에 보내주어야하는데, await을 통해서 api 도메인을 받아올 때까지 
기다려야함.

-그리고 params를 통해서 각각 다른 기본값(공공데이터 내 params)들을 설정해줌. 


///30:16 index.html 실행단계 